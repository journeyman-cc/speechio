(ns cc.journeyman.speechio.core-test
  (:require [clojure.test :refer [deftest is testing]]
            [cc.journeyman.speechio.core :refer :all]))

(deftest a-test
  (testing "Nothing to see here (yet)."
    (is (= 1 1))))
